/**
 * Everything email
 */

module.exports = {
  sendConfirmationEmail: function emailSendConfirmationEmail (email, id, callback) {
    // TODO: Actually send a confirmation email
    callback(null);
  }
}